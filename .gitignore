# 환경 변수 (절대 커밋하면 안 됨!)
.env
.env.local
*.env

# 자격 증명
credentials.json
token.json
*.pem
*.key

# 임시 파일
.tmp/
*.tmp
.DS_Store

# 런타임 로그/상태 (자동화가 갱신 — 추적 제외, 로컬에서만 누적)
data/monitor_log.json
data/realtime_state.json
data/weekly_todos_log.json
data/notion_archive_state.json

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db

# reels_studio (릴스 auto 모드 영상 엔진)
# 생성된 영상·음성·중간 산출물 (대용량)
reels_studio/output/
reels_studio/__pycache__/
# 단일 .env 원칙 — 루트 .env로 통합 예정
reels_studio/.env
# BGM 라이브러리 (45MB mp3) — 엔진 동작엔 필요하나 git 이력 오염 방지차 제외
reels_studio/bgm/
