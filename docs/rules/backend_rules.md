# backend_rules.md
# 백엔드 개발 표준 및 규칙 가이드

1. **에러 처리:** 모든 비동기 컨트롤러 및 서비스 로직은 반드시 적절한 전역 에러 핸들러(`try-catch` 또는 미들웨어)로 감싸야 한다.
2. **RESTful 규칙:** URI는 복수 명사를 사용하며, 행위는 HTTP Method(GET, POST, PUT, DELETE)로 명확히 분리한다.
3. **로그 기록:** 주요 예외 상황이나 민감한 트랜잭션 발생 시 무조건 로그 레벨(info, error)에 맞추어 로깅을 수행한다.